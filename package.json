{
  "name": "all-eslint-rules",
  "version": "2.1.1",
  "description": "An array of the all available ESLint rules",
  "repository": "shinnn/all-eslint-rules",
  "scripts": {
    "pretest": "eslint --fix --parser babel-eslint --config @shinnn index.js test.js",
    "test": "node --throw-deprecation --track-heap-objects node_modules/.bin/ava --verbose"
  },
  "license": "MIT",
  "files": [
    "index.js"
  ],
  "keywords": [
    "eslint",
    "setting",
    "config",
    "configs",
    "configuration",
    "arr",
    "ary",
    "array",
    "list",
    "rule",
    "rules"
  ],
  "dependencies": {
    "eslint": "^3.0.0"
  },
  "devDependencies": {
    "@shinnn/eslint-config": "^2.4.0",
    "array-difference": "^0.0.1",
    "ava": "^0.15.2",
    "babel-eslint": "^6.1.0",
    "fetch-cheerio-object": "^1.0.1"
  }
}
